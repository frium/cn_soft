<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fyy.mapper.ScoreMapper">
    <select id="getAllStudentsScores">
        select student.name, score.*
        from student
                 join score on score.student_id = student.student_id
        where student.teacher_id = #{ID}
          and score.title like CONCAT('%', #{title}, '%')
        order by score.title
        limit #{offset}, #{pageSize};
    </select>
    <select id="getStudentScores">
        select *
        from score
        where student_id = #{ID};
    </select>
    <select id="getAllScores">
        select distinct score.title
        from score
        order by score.title
        limit #{offset}, #{pageSize};
    </select>
</mapper>
